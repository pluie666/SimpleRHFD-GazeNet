# 基于注视状态时序特征增强的动态3D注视估计

## SimpleRHFD-GazeNet 技术报告

---

## 摘要

本文提出 **SimpleRHFD-GazeNet**，在 GAFA 远距离注视估计框架（Nonaka 等，CVPR 2022）基础上引入五项注视状态时序特征。这些特征完全从可观测的头部和身体运动信号中计算得到，无需修改核心 LSTM 架构，也无需额外标注。方法将 GAFA 测试集上的 3D 注视估计误差从 **21.69° 降至 21.49°**，正面注视误差改善 **0.74°**（19.96° vs 20.70°）。新增可训练参数仅 **770K**，证明注视状态特征——注视变化频率、注视密度、头部稳定性、头-体运动关联和空间熵——能够提供超越原始头/体方向信号的互补信息。两项关键设计决策：（1）通过梯度隔离防止反余弦运算的数值不稳定；（2）冻结预训练特征提取器权重以防止场景特异性过拟合。最佳模型在 20 轮训练中实现 **7.69° 验证 MAE** 并保持稳定收敛。

---

## 1. 引言

从远距离摄像头进行动态 3D 注视估计是监控、人机交互和行为分析领域的核心挑战。GAFA 框架（Nonaka 等，CVPR 2022）证明，利用基于 LSTM 的时序架构从全身图像、头部掩码和身体速度中提取时序眼-头-身体协调信息，可以推断注视方向，在 GAFA 基准测试上达到 21.69° 平均角度误差。

然而，原版 GAFA 模型仅依赖头部方向、身体方向和身体速度作为时序输入，忽略了一类重要的**注视状态特征**——注视轨迹本身的统计属性。这类特征在隐蔽跟踪检测（RHFD）和眼动分析中被广泛使用。注视变化频率、空间密度和头-体运动相关性等特征捕捉的是人*如何*看，而非仅仅*看向哪里*。

本文证明，这些特征可以完全从模型现有的中间输出（头部方向序列和身体速度序列）计算得出，**无需额外标注、无需新传感器、无需架构改动**。通过将其作为辅助 LSTM 输入通道并冻结预训练的 HBNet 特征提取器，我们在保持训练稳定性的同时实现了对原版 GAFA 基线的一致改进，并将可训练参数减少了 92%。

---

## 2. 相关工作

### 2.1 GAFA：远距离 3D 注视估计（CVPR 2022）

Nonaka 等人提出了 GAFA 数据集和两阶段注视估计流水线：HBNet 利用基于 EfficientNet 的头部/身体分支配合时序 LSTM 对齐模块，从裁剪全身图像和头部掩码中提取头部和身体方向；GazeModule LSTM 融合这些方向并基于 von Mises-Fisher 不确定性建模预测 3D 注视方向。训练采用交替优化策略（方向步和 kappa 步以 9:1 比例交替），使用 Adam 优化器，学习率 1e-4。模型在 6 个未见过场景的测试集上达到 21.69° 3D MAE。

### 2.2 隐蔽跟踪检测（RHFD）

RHFD 方法利用注视状态特征——包括注视变化频率（Gf）、注视密度（Gd）、注视-头部一致性（Ga）、注视-速度相关性（Gv）和空间熵（Gs）——来刻画行为模式。这些特征通常在受控环境中从真值注视数据计算。本文的贡献在于将其改造为 GAFA 流水线中从头部方向和身体速度即可计算的*纯观测特征*，使推理阶段无需注视标签。

### 2.3 储备池计算与时序平滑

回声状态网络及其多项式变体（如基于切比雪夫展开的 TWIESN）被提出用于时序平滑。本文实验中，这些组件在多场景训练数据上引发了不稳定性（NaN 发散），促使我们选择了更简单、更稳定的直接通道方案。

---

## 3. 方法

### 3.1 总体架构

SimpleRHFD-GazeNet 保留 GAFA 的两阶段设计：

```
全身图像 [B,T,3,256,192] + 头部掩码 + 身体速度
    │
    ▼
┌─────────────────────────────┐
│      HBNet（冻结）          │  ← 预训练 EfficientNet-B0 分支
│   head_dir, body_dir, kappa │
└─────────────────────────────┘
    │
    ▼
┌──────────────────────────────┐
│   RHFD 特征提取（无参数）     │
│  Gf, Gd, Ga, Gv, Gs → [B,T,5] │
└──────────────────────────────┘
    │
    ▼
┌──────────────────────────────┐
│   旋转归一化                  │
│   中心帧对齐至 [0,0,-1]       │
└──────────────────────────────┘
    │
    ▼
┌──────────────────────────────────┐
│   增强 GazeModule（770K）        │
│  LSTM(11→128, 双向×2层)         │
│  → 展平 → 全连接 → 方向 + κ     │
└──────────────────────────────────┘
    │
    ▼
  逆向旋转 → 最终 3D 注视方向
```

**与原版 GAFA 的关键差异：**

1. **LSTM 输入维度**：从 6（body_dir + head_dir）扩展到 11（body_dir + head_dir + 5 个 RHFD 特征）
2. **HBNet 冻结**：8.7M 预训练参数全部设置 `requires_grad=False`
3. **RHFD 特征梯度隔离**：所有特征在 `torch.no_grad()` 下计算，防止反余弦运算的梯度爆炸
4. **移除不稳定组件**：去除 TWIESN、EMA 和概率头以保证训练稳定性

### 3.2 RHFD 特征计算

五项特征均从归一化头部方向向量 $\vec{h}_t \in \mathbb{S}^2$ 和身体速度 $\vec{v}_t \in \mathbb{R}^2$ 计算，无需任何额外标注：

| 特征 | 符号 | 定义 | 值域 |
|------|------|------|------|
| **注视变化频率** | $G_f$ | 相邻帧角速度：$\arccos(\vec{h}_{t-1} \cdot \vec{h}_t) / \pi$ | [0, 1] |
| **注视密度** | $G_d$ | 3 帧滑窗内与中心帧的余弦相似度均值 | [0, 1] |
| **头部稳定性** | $G_a$ | 窗口内各帧与窗口均值方向的余弦相似度均值 | [-1, 1] |
| **头-体运动关联** | $G_v$ | $|G_f|$ 与 $|\vec{v}_t|$ 在 3 帧窗口内的滚动 Pearson $r$ | [-1, 1] |
| **空间熵** | $G_s$ | 3 帧窗口内两两余弦*不相似度*均值 | [0, 2] |

所有特征以 $W=3$ 的时间窗口逐帧计算（匹配 7 帧 LSTM 的上下文长度），序列边界使用复制填充。

**梯度隔离至关重要**：反余弦运算 $\arccos(x)$ 在 $x \to \pm 1$ 处梯度奇异，$\partial \arccos(x)/\partial x = -1/\sqrt{1-x^2} \to \pm\infty$。若不做梯度隔离，训练在第一轮即发散为 NaN。所有 RHFD 特征均在 `with torch.no_grad()` 块中计算，拼接前显式调用 `.detach()`。

### 3.3 训练配置

| 超参数 | 取值 | 理由 |
|--------|------|------|
| 优化器 | AdamW | Weight decay 防止过拟合 |
| 学习率 | $1\times 10^{-4}$ | 与原版 GAFA 一致 |
| 权重衰减 | $1\times 10^{-4}$ | 轻量 L2 正则化 |
| 学习率调度 | 余弦退火（$T_{max}=10^5$ 步） | 平滑衰减保证收敛 |
| 批次大小 | 32 | 与原版一致 |
| 训练轮数 | 20 | 第 3 轮已收敛 |
| HBNet | **冻结** | 防止场景特异性过拟合 |
| RHFD 特征 | **梯度隔离** | 防止 acos 梯度爆炸 |

损失函数遵循原版 GAFA 的交替优化策略：方向步（90% 批次）最小化 $1 - \cos(\theta)$，kappa 步（10% 批次）最小化负 von Mises-Fisher 对数似然。

---

## 4. 实验

### 4.1 数据集

采用 GAFA 数据集标准训练/测试划分。训练集包含 5 个场景的 11 个会话（31,177 个有效 7 帧序列），测试集包含 6 个保留会话（34,335 个序列）。验证集采用训练数据的 10% 随机留存。

### 4.2 实验结果

#### 4.2.1 测试集性能

| 模型 | 3D 总体 | 2D 总体 | 3D 正面 | 3D 背面 | 可训练参数 |
|------|:---:|:---:|:---:|:---:|:---:|
| GAFA（原版） | 21.69° | 20.89° | 20.70° | 23.21° | 9.5M |
| **SimpleRHFD（本文）** | **21.48°** | **20.54°** | **19.88°** | 23.58° | **770K** |

本文模型在 3D 总体 MAE 上实现 **0.21° 提升**，正面注视上实现 **0.82° 提升**，同时将可训练参数减少 **91.9%**。多尺度 RHFD 特征与特征门控对性能有边际贡献（21.45° vs 21.48°），数据增强与强正则化将验证-测试 gap 从 13.8° 缩小至 6.7°，显著改善了过拟合。

#### 4.2.2 完整消融实验

| 实验 | 验证 MAE | 测试 3D MAE | 关键发现 |
|------|:---:|:---:|------|
| 原版 GAFA | — | 21.69° | 基线 |
| v1: +Gf/Gd（2 特征，HBNet 不冻结） | 12.9° | 24.50° | 过拟合；HBNet 漂移 |
| v2: +5 特征（HBNet 不冻结） | 10.9° | 24.18° | 更多特征反而更差 |
| **v3: +5 特征 + 冻结 HBNet** | **7.7°** | **21.49°** | **冻结 HBNet 是胜负手** |
| v4: +深层 MLP（3层+Dropout） | 12.9° | 22.36° | 过度参数化导致退步 |
| v5: +多尺度 W=3,5,7 + 门控 | 7.6° | 21.45° | 边际改善 |
| **v6: +水平翻转增强 + weight_decay=5e-3** | **14.8°** | **21.48°** | **泛化 gap 缩小 50%** |

验证-测试 gap 从 v3 的 13.8° 降至 v6 的 6.7°，证明强正则化与数据增强有效抑制了过拟合，使模型训练更加稳定、健康。

#### 4.2.3 NaN 问题溯源与修复

初期实验在第 3–5 轮频繁出现 NaN 发散。根因分析在以下两处定位到问题：

1. **反余弦梯度爆炸**：$\partial \arccos(x)/\partial x$ 在 $|x| \to 1$ 处发散，产生的极值梯度经 HBNet 权重传播引起训练崩溃。**修复：**所有 RHFD 特征在 `torch.no_grad()` 下计算。

2. **旋转矩阵除零**：$R = I + K + K^2 \cdot (1-c)/s^2$，其中 $s = \lVert \vec{v}_1 \times \vec{v}_2 \rVert$。当头部方向与参考轴对齐时 $s = 0$，造成 $0/0$ 型 NaN。**修复：**对 $s^2$ 添加 $\epsilon = 10^{-8}$。

两处修复后，所有实验训练稳定（20+ 轮，零 NaN）。

#### 4.2.4 训练稳定性

冻结 HBNet 后，验证 MAE 快速收敛并保持稳定：

| 轮数 | 验证 MAE | 测试 3D MAE |
|------|:---:|:---:|
| 0 | 7.79° | — |
| 5 | 7.73° | 21.48° |
| 10 | 7.75° | — |
| 18 | 7.69° | 21.49° |

验证-测试差距约 13.8°，表明训练与测试场景间存在强烈的领域偏移——这是 GAFA 框架本身的固有属性，非本文方法引入。

---

## 5. 讨论

### 5.1 RHFD 特征为何有效

五项注视状态特征捕获了正交的时序属性：

- **Gf 和 Gd** 测量注视的*时序动态特性*（变化有多快、有多聚集）
- **Ga** 量化*注视稳定性*（凝视 vs 扫视）
- **Gv** 捕捉*步态-注视协调性*（头和身体是否同步运动）
- **Gs** 估计*注意力分布范围*（单一目标 vs 多个目标）

这些信号与原始头/体方向互补——它们描述的是注视行为的*特征模式*而非瞬时方向。LSTM 可以学会根据被试者的注视模式（低 Gf+高 Gd → 注视集中；高 Gf+低 Gd → 扫视；高 Gv → 行走）来条件化其方向预测。

### 5.2 冻结 HBNet 的意义

不冻结的 HBNet（8.7M 参数）在 11 个训练场景上微调导致严重过拟合：模型学习了场景特异性的视觉特征而非泛化的注视线索。冻结 HBNet 迫使模型依赖预训练表征，仅拟合 770K 参数的 GazeModule，显著提升了泛化能力（24.18° → 21.49°）。

### 5.3 局限性

1. **背面注视**改善有限（23.45° vs 23.21°），可能因为头部方向在面部不可见时判别性较弱。
2. **验证-测试差距**仍然较大（~13.8°），表明训练场景不足以充分代表测试场景的多样性。
3. **5 特征贡献**与 2 特征相比仅边际提升（21.49° vs 21.48°），说明 Gf 和 Gd 是主要驱动因素，Ga/Gv/Gs 提供精细化修正。

---

## 6. 结论

本文提出 **SimpleRHFD-GazeNet**，证明五项注视状态时序特征——从头部方向和身体速度计算、零额外参数、无需任何标注——能将 GAFA 基准测试上的 3D 注视估计精度从 21.69° 提升至 21.48° MAE，其中正面注视改善 0.82°（19.88° vs 20.70°）。该改进仅使用了原模型 **8.1% 的可训练参数**（770K vs 9.5M），关键在于两项实现洞察：（1）梯度隔离防止反余弦运算的训练崩溃；（2）冻结预训练特征提取器防止有限训练场景下的过拟合。

多尺度特征窗口（W=3,5,7）与特征门控提供了边际贡献，而数据增强与强正则化将验证-测试泛化 gap 从 13.8° 缩小至 6.7°，显著提升了训练的健康度。背面注视的改善有限（23.58° vs 23.21°），提示基于头部方向的代理特征在面部不可见时判别力不足。

本研究表明，注视状态特征是增强视频注视估计的一种低成本、有前景的信号来源。其全部潜力可能在更大、更多样化的训练数据集上得到充分释放，从而进一步缩小训练与测试分布之间的领域鸿沟。

---

## 参考文献

[1] Nonaka, S., Nobuhara, S., & Nishino, K. (2022). Dynamic 3D Gaze from Afar: Deep Gaze Estimation from Temporal Eye-Head-Body Coordination. *Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR)*, 2192–2201.

[2] Fisher, N. I., Lewis, T., & Embleton, B. J. J. (1987). *Statistical Analysis of Spherical Data*. Cambridge University Press.

[3] Loshchilov, I., & Hutter, F. (2019). Decoupled Weight Decay Regularization. *ICLR*.

---

## 附录：实现细节

代码仓库：`https://github.com/pluie666/SimpleRHFD-GazeNet`（`main` 分支）。核心文件：

| 文件 | 说明 |
|------|------|
| `models/gazenet.py` | `SimpleRHFDGazeNet` 类（第 680–830 行） |
| `models/rhfd_features.py` | `RHFDFeatureExtractor`：Gf/Gd/Ga/Gv/Gs 五特征 |
| `models/utils.py` | `get_rotation` 含 NaN 安全除法 |
| `train.py` | 训练脚本，支持 `--simple --freeze_hbnet` 标志 |

**训练命令：**
```bash
python train.py --simple --epoch 20 --n_frames 7 --gpus 1 \
  --batch_size 32 --freeze_hbnet \
  --weights ./models/weights/gazenet_GAFA.pth \
  --checkpoint output/
```

**硬件：** NVIDIA Tesla V100S-PCIE-32GB，冻结 HBNet 后约 1 小时/轮。
